extends base
include mixins/video

block content   
    div.video-grid
        //- video can be anything like potato, item, videos should be videos!
        each video in videos 
            //- iteration(반복)
            +video(video)
        else 
            span.empty__message No video found
        